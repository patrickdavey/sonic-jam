<%= form_for(@buffer) do |f| %>
  <% if @buffer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@buffer.errors.count, "error") %> prohibited this buffer from being saved:</h2>

      <ul>
      <% @buffer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_area :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
